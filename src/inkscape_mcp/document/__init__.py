"""Document model layer: open + direct-DOM inspection.

`inspect` holds the reusable inspection engine (pure functions returning Pydantic
models, no MCP decorators). The MCP tool layer lives in `inkscape_mcp.tools.document`.
"""
